
# 文件名: .github/workflows/sync-tn-c3-newest.yaml
name: Sync sunnypilot tn-prebuilt to tn-C3XL-newest

on:
  workflow_dispatch:

env:
  UPSTREAM_REPO: "sunnypilot/sunnypilot"
  UPSTREAM_BRANCH: "tn-prebuilt"
  MY_REPO: "${{ github.repository }}"
  TARGET_BRANCH: "tn-prebuilt"

jobs:
  sync-job:
    runs-on: ubuntu-latest
    env:
      GIT_LFS_SKIP_SMUDGE: 1
    permissions:
      contents: write
      actions: write # 显式声明权限

    steps:
      # 1. 检出 Upstream 源码
      - name: Checkout upstream
        uses: actions/checkout@v4
        with:
          repository: ${{ env.UPSTREAM_REPO }}
          ref: ${{ env.UPSTREAM_BRANCH }}
          fetch-depth: 0
          filter: blob:none
          lfs: false

      # 2. 执行 Cherry-pick 并彻底删除工作流文件
      - name: Apply modifications and remove workflows
        run: |
          # 禁用 LFS 钩子
          git config --global filter.lfs.smudge "cat"
          git config --global filter.lfs.clean "cat"
          git config --global filter.lfs.required false
          git lfs uninstall --local || true

          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

          # 添加修改源（你的仓库）
          git remote add mod_source "https://github.com/${{ github.repository }}.git"
          git fetch mod_source --no-tags

          echo "Performing fixed range cherry-pick..."
          git cherry-pick 04f1b0b708206e14058ec7b0756b0d7ac1800058 -X theirs
          git cherry-pick 3214a50699b14b0991f9378762409fa3b68d08bf -X theirs
          git cherry-pick aae3631bd8041e9d0f0c60eb6d1d04306f2b1348 -X theirs

    
      # 3. 推送到目标分支
      - name: Force push to target branch
        env:
          GIT_LFS_SKIP_PUSH: 1
        run: |
          echo "Force pushing to target branch..."

          # 使用 HEAD:refs/heads/tn 确保无论本地叫什么都推送到 tn
          git -c lfs.hooksonly=false push "https://x-access-token:${{ secrets.PERSONAL_ACCESS_TOKEN }}@github.com/${{ github.repository }}.git" \
            HEAD:refs/heads/${{ env.TARGET_BRANCH }} --force --no-thin --no-verify

